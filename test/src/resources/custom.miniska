var Character = import "custom_base";

var player = Character.build("Player");
var enemy = Character.default;

var totalAge = 0;

for(var i = 0; i < enemy.age; i = i + 1) {
	totalAge = totalAge + 1;
}
totalAge = totalAge + enemy.age + player.age;
