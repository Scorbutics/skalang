IOLog = import "bind:std.native.io.log"
Defer = import "defer_test"

run = function(parameters: string[]) do
	callback = function(result: int) do
		IOLog.print("defered print with : " + result: string)
	end
	Defer.defer(parameters[0] : int, parameters[1] : int, parameters[2] : int, callback)
end
