IOLog = import "bind:std.native.io.log"

run = function(parameters: string[]) do
    TestFcty = function(value_: string): var do
        TestFctyPrivateFcty = function(): var do
            var test = "tututu";
            return {
                value_ = value_
                test = test
            }
        end      
        return {
            privateData = TestFctyPrivateFcty()
            value = function(self: TestFctyPrivateFcty()): string do
                return self.value_
            end
        }
    end

    toto = TestFcty("toto")
    titi = TestFcty("titi")

    IOLog.print(toto.value(toto.privateData) + titi.value(titi.privateData))
end
