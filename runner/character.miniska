var Position = import "point";

var build = function(name: string) : var {
    var defaultPos = Position.build(0, 0);
    return {
        name : name,
        direction : 0,
        pos : defaultPos
    };
};


